#[allow(unused)]
use super::*;

pub fn movcf2gr(code: u32, address: u64, symbol: &HashMap<u64, SymbolRecord>) -> AssemblyInstruction {
    let mut assembly_instrction = AssemblyInstruction::new();

    assembly_instrction
}
