#[allow(unused)]
use super::*;

pub fn movgr2cf(code: u32, address: u64, symbol: &HashMap<u64, SymbolRecord>) -> AssemblyInstruction {
    let mut assembly_insruction = AssemblyInstruction::new();

    assembly_insruction
}
