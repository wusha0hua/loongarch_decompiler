#[allow(unused)]
use super::*;

pub fn movgr2cf(code: u32, address: usize, symbol: HashMap<usize, SymbolRecord>) -> AssemblyInstruction {
    let mut assembly_insruction = AssemblyInstruction::new();

    assembly_insruction
}
