#[allow(unused)]
use super::*;

pub fn movcf2gr(code: u32, address: usize, symbol: HashMap<usize, SymbolRecord>) -> AssemblyInstruction {
    let mut assembly_instrction = AssemblyInstruction::new();

    assembly_instrction
}
